openapi: 3.0.0
info:
  version: 0.1.0
  title: Test

components:
  schemas:
    Top:
      type: object
      properties:
        bar:
          $ref: "#/components/schemas/Sub"
    Sub:
      type: object
      properties:
        foo:
          type: string
