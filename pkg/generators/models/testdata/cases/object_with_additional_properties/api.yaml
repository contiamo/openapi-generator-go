openapi: 3.0.0
info:
  version: 0.1.0
  title: Test

components:
  schemas:
    Foo:
      type: object
      properties:
        foo:
          type: object
          additionalProperties:
            type: string

    FooBar:
      type: object
      additionalProperties: false

    Bar:
      type: object
      additionalProperties:
        type: string
        format: email

    Baz:
      type: object
      additionalProperties:
        $ref: '#/components/schemas/Foo'
