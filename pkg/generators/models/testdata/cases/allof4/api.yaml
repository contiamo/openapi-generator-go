openapi: 3.0.0
info:
  version: 0.1.0
  title: Test

components:
  schemas:
    Foo:
      type: object
      properties:
        bar:
          nullable: true
          allOf:
            - $ref: "#/components/schemas/Sub"

    Sub:
      type: object
      properties:
        sub:
          type: string
