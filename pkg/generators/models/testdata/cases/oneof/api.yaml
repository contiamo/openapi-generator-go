openapi: 3.0.0
info:
  version: 0.1.0
  title: Test

components:
  schemas:
    Foo:
      type: object
      properties:
        bar:
          oneOf:
            - type: string
            - type: integer
